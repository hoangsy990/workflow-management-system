[package]
name = "workflow-management-system"
version = "0.1.0"
description = "WorkFlow Management System native shell"
authors = ["WorkFlow"]
license = "UNLICENSED"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

