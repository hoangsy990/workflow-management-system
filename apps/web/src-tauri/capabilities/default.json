{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for WorkFlow desktop and mobile shell.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}

